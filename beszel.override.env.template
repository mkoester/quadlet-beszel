# Local overrides — values here take precedence over beszel.env.

# Required: the URL where Beszel will be accessed (used for OAuth callbacks and links in notifications)
APP_URL=https://beszel.example.com

# Optional: override timezone
#TZ=Europe/Berlin
